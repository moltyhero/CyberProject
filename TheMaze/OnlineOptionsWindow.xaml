<Window x:Class="TheMaze.OnlineOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheMaze"
        mc:Ignorable="d"
        Title="OnlineOptionsWindow" Height="800" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Button Name="hostButton" Grid.Column="0" Content="Host" Click="Host_Click"></Button>
        <Button Name="connectButton" Grid.Column="1" Content="Connect" Click="Connect_Popup_Click"></Button>

        <Popup Name="connectPopup" Width="500" Height="500" Placement = "Center" IsOpen="False">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <TextBox Name="hostIPTextBox" Text="Enter the Host IP" Grid.Column="0"/>
                <Button Grid.Column="1" Content="Connect" Click="Connect_Click"/>
            </Grid>
        </Popup>
    </Grid>
</Window>
